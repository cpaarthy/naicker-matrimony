# Registration Age Save Fix

Fixed registration/edit validation so the selected Age dropdown value is normalized, validated as 18–70, and persisted as a number before profile save. Registration now reports actual profile-save errors instead of showing success when the save fails.
